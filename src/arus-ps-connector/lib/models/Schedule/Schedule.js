/**
 * Serves as the model for Schedule data
 *
 * @class
 */
class Schedule {

   constructor(scheduleData) {
     let fields = {
       terms: this.terms
     } = scheduleData;
   }
}

module.exports = Schedule;
